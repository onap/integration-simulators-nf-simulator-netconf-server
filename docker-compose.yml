version: '3'

services:

  netconf-server:
    container_name: netconf-server
    image: onap/org.onap.integration.nfsimulator.netconfserver:latest
    environment:
     - ENABLE_TLS=true
#    volumes:
#     - ./demo_models:/resources/models
#     - ./demo_certs:/resources/certs
    ports:
     - "830:830"
     - "6513:6513"
